<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.tutoquizzer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EditTopicFragment"
            android:parentActivityName=".TutorialFragment" />
        <activity
            android:name=".EditCourseFragment"
            android:parentActivityName=".AddCourseFragment" />

        <activity android:name=".QuizFragment" />

        <activity android:name=".TutorialFragment" />

        <activity
            android:name=".SelectCourseFragment"
            android:parentActivityName=".MainFragment" />

        <activity android:name=".AddCourseFragment" />

        <activity android:name=".AddTopicFragment" />

        <activity android:name=".MainFragment">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>